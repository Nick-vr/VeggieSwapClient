export interface TradeItem {
  id: number,
  userId: string,
  userFirstName: string,
  userLastName: string,
  amount: number,
}

interface Resource {
  id: number,
  name: string,
  imageUrl: string,
}